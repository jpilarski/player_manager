<Window x:Class="Pilarski.PlayerManager.UI.ClubWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:Pilarski.PlayerManager.Core;assembly=Pilarski.PlayerManager.Core"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Club Details" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <ObjectDataProvider x:Key="CountriesData" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="core:Country"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="CLUB NAME" FontWeight="Bold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding Name}" CharacterCasing="Upper" Padding="5"/>

            <TextBlock Text="COUNTRY" FontWeight="Bold" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource CountriesData}}" 
                      SelectedItem="{Binding Country}" Padding="5"/>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="FOUNDING YEAR" FontWeight="Bold"/>
                    <TextBox Text="{Binding FoundingYear}" Padding="5"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="STADIUM" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Stadium}" CharacterCasing="Upper" Padding="5"/>

            <TextBlock Text="LOGO FILENAME (e.g. barca.png)" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBox Text="{Binding ImagePath}" Padding="5"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnSave" Content="SAVE" Width="100" Height="30" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="CANCEL" Width="100" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>